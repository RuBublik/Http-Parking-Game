# HTTP-Parking-Game

College assignment.

## Requirements

- Node.js 18 or newer

## Run

```bash
npm install
npm start
```

Then open http://localhost:8000

- `/` — the game
- `/schemas` — the resources and their fields

For dev. - `npm run dev` restarts the server when files change.
